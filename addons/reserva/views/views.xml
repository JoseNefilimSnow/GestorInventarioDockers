<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="reserva.list">
      <field name="name">reserva list</field>
      <field name="model">reserva.reserva</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="reserva.action_window">
      <field name="name">reserva window</field>
      <field name="res_model">reserva.reserva</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="reserva.action_server">
      <field name="name">reserva server</field>
      <field name="model_id" ref="model_reserva_reserva"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="reserva" id="reserva.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="reserva.menu_1" parent="reserva.menu_root"/>
    <menuitem name="Menu 2" id="reserva.menu_2" parent="reserva.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="reserva.menu_1_list" parent="reserva.menu_1"
              action="reserva.action_window"/>
    <menuitem name="Server to list" id="reserva" parent="reserva.menu_2"
              action="reserva.action_server"/>
    -->
  </data>
</odoo>